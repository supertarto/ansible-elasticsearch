---
language: python
services: docker

env:
  global:
    - ROLE_NAME: elasticsearch
  matrix:
    - MOLECULE_DISTRIB: debian10
    - MOLECULE_DISTRIB: debian9

install:
  - pip install molecule docker

before_script:
  - cd ../
  - mv ansible-$ROLE_NAME supertarto.$ROLE_NAME
  - cd supertarto.$ROLE_NAME

script:
  - molecule test

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
